# TODO

* Write README.md
* Allow keeping the .MOV from .zip files and further settings for zip extraction
* Prevent photo overwrite
* Investigate separationg of `generate_entry` and `get_entry_string`
* Allow for "missing days" by implementing better search
* Statistics
* Make journals H2 and add <month name> <year> as H1.
* Update previous journals to have real headings matching european date format